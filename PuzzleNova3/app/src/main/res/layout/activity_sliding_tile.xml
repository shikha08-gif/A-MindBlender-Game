<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#1E1B4B"
    android:padding="16dp"
    tools:context=".SlidingTileActivity">

    <!-- Title -->
    <TextView
        android:id="@+id/title_text_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:text="Sliding Puzzle"
        android:textColor="#00ffff"
        android:textSize="34sp"
        android:textStyle="bold" />

    <!-- Moves Counter -->
    <TextView
        android:id="@+id/moves_text_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/title_text_view"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="8dp"
        android:text="Moves: 0"
        android:textColor="#88aaff"
        android:textSize="22sp"
        android:textStyle="bold"/>

    <!-- Reset and New Game Buttons -->
    <LinearLayout
        android:id="@+id/button_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:orientation="horizontal">

        <Button
            android:id="@+id/reset_button"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Reset (Shuffle)"
            android:backgroundTint="#003366"
            android:textColor="#00ffff"
            android:layout_marginEnd="8dp"
            android:paddingVertical="12dp"
            android:textSize="16sp"/>

        <Button
            android:id="@+id/new_game_button"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="New Game"
            android:backgroundTint="#003366"
            android:textColor="#00ffff"
            android:layout_marginStart="8dp"
            android:paddingVertical="12dp"
            android:textSize="16sp"/>
    </LinearLayout>

    <!-- The Custom Game Board -->
    <com.example.puzzlenova.SlidingTileView
        android:id="@+id/sliding_tile_view"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_above="@id/button_bar"
        android:layout_below="@id/moves_text_view"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="16dp"
        android:layout_centerHorizontal="true" />

</RelativeLayout>
